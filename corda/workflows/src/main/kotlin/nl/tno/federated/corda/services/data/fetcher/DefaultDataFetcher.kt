package nl.tno.federated.corda.services.data.fetcher

class DefaultDataFetcher {
    fun fetch(sparql: String): String? {
        return "TODO: Need to implement fetching data from an external resource"
    }
}